# Environment configuration
# Set the ENV variable to specify the environment:
# Options: DEV, TEST, PROD
# Configuration files for each environment are located in the /config directory:
# - /config/dev.yaml for development
# - /config/test.yaml for testing
# - /config/prod.yaml for production
ENV=DEV
